plugin:
  redis_cache:
    backend:
      info:
        name:
          other: Redis Cache
        description:
          other: Use Redis as cache
      config:
        endpoint:
          title:
            other: Endpoint
          description:
            other: Redis connection address, such as 127.0.0.1:6379
